# Unified deep-reach API
PORT=8320
HOST=0.0.0.0
# Upstreams (deep-reach-worker / paperbot)
WORKER_URL=http://localhost:8321
PAPERBOT_URL=http://localhost:8322
# Optional: cap how long the glue service will wait on a single upstream call
UPSTREAM_TIMEOUT_MS=300000
